{
  "name": "portfolio",
  "version": "1.0.0",
  "main": "index.html",
  "scripts": {
    "start": "concurrently \"npm run sass\" \"npm run serve\"",
    "sass": "sass --watch scss:css",
    "serve": "browser-sync start --server --files 'index.html, css/*.css, js/*.js, scss/*.scss'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "concurrently": "^9.1.2",
    "sass": "^1.87.0"
  }
}
